name: laravel-tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: laravel
          POSTGRES_PASSWORD: laravel
          POSTGRES_DB: laravel_test
        ports: [5432:5432]
        options: >-
          --health-cmd "pg_isready -U laravel"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP 8.2
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pgsql, bcmath, zip
          coverage: none

      - name: Copy .env
        run: cp .env.example .env

      - name: Install Composer dependencies
        run: composer install --prefer-dist --no-progress --no-suggest --no-interaction

      - name: Generate app key
        run: php artisan key:generate

      # INI YANG DIUBAH
      - name: Install NPM dependencies & Build assets
        run: |
          npm install          # ← dari npm ci jadi npm install
          npm run build

      - name: Run Tests
        env:
          DB_CONNECTION: pgsql
          DB_HOST: localhost
          DB_PORT: 5432
          DB_DATABASE: laravel_test
          DB_USERNAME: laravel
          DB_PASSWORD: laravel
        run: php artisan test --coverage

      - name: Laravel Pint (optional)
        run: ./vendor/bin/pint --test || true

      - name: PHPStan (optional)
        run: ./vendor/bin/phpstan analyse --no-progress || true